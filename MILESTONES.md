# 复古游戏博物馆开发里程碑

## 总目标

完成一个本地单用户 Web App，用于管理游戏资料、随记、截图和存档节点。

第一版不实现存档内容修改。存档修改作为后续独立能力，等资料管理功能稳定后再设计和实现。

## Milestone 0：项目基础

目标：建立可以启动和验证配置的本地 Web App。

任务：

- 确定项目运行方式和目录结构。
- 实现 `~/.config/game_museum/config.yaml` 读取。
- 校验 YAML、必需字段、绝对路径和目录存在性。
- 配置错误时在终端输出错误并退出。
- 默认监听 `127.0.0.1:7007`。
- 启动时不自动打开浏览器。
- 建立中文界面基础布局。

验收：

- 配置正确时应用能够启动。
- 配置缺失或错误时应用不会启动，并能看到明确错误。

## Milestone 1：游戏资料目录和 Markdown

目标：能够创建、发现和读取游戏资料。

任务：

- 实现平台预设列表和下拉菜单。
- 实现游戏名清理规则。
- 实现平台与清理后游戏名的唯一性检查。
- 创建游戏资料目录、`game.md`、`screenshots/` 和 `saves/`。
- 生成固定的四个 Markdown section。
- 扫描资料库并识别合法游戏目录。
- 忽略没有 `game.md` 的目录。
- 忽略存在 `.archived` 的目录。
- 实现 Markdown section 解析。
- 保存时只更新应用管理的 section，并保留未知内容。

验收：

- 可以创建游戏并生成正确目录。
- 刷新后可以从资料库重新发现游戏。
- 外部修改 `game.md` 后，手动刷新能读取最新内容。
- 保存应用内容不会破坏未知 section 或文字。

## Milestone 2：游戏列表和详情页

目标：完成基本浏览和资料编辑闭环。

任务：

- 实现主页游戏列表。
- 显示游戏名和平台。
- 按游戏名忽略大小写排序，同名时按平台排序。
- 实现游戏名片段搜索，忽略大小写。
- 实现新建游戏表单。
- 实现详情页和返回列表。
- 显示基本资料、简介、随记、截图、存档节点和存档文件。
- 实现简介和随记编辑保存。
- 空内容显示“没有”。
- 实现修改游戏名、平台和简介。
- 修改身份字段时同步移动目录和更新 `game.md`。

验收：

- 可以从主页找到游戏并进入详情。
- 可以创建、修改和刷新游戏资料。
- 搜索、排序和空状态符合需求。

## Milestone 3：存档目录和存档节点管理

目标：管理存档文件及其进度节点，但不修改存档内容。

任务：

- 实现存档目录路径输入和自动补全。
- 校验填写的路径必须是已存在的目录。
- 支持资料库内和资料库外的存档目录。
- 扫描存档目录第一层普通文件。
- 在详情页显示存档文件名。
- 点击文件名复制完整路径。
- 实现新增、编辑和删除存档节点。
- 节点支持可选的存档文件关联。
- 节点信息写入 `## 存档节点`。
- 处理关联文件不存在的情况。
- 删除节点时不删除实际存档文件。

验收：

- 可以绑定存档目录并看到其中已有文件。
- 可以创建没有文件关联的节点。
- 可以关联、修改和删除节点。
- 外部删除文件后页面显示“文件不存在”。

## Milestone 4：截图搜刮和展示

目标：完成从全局截图目录到游戏资料目录的整理闭环。

任务：

- 扫描全局截图目录第一层。
- 支持 PNG、JPG/JPEG 和 WebP。
- 实现 5 分钟、10 分钟、30 分钟、60 分钟和 5 小时回溯选项。
- 按文件修改时间筛选当前时间之前的图片。
- 将图片移动到当前游戏的 `screenshots/`。
- 处理目标文件重名并追加递增序号。
- 显示移动成功和忽略文件的结果。
- 没有结果时显示“没有找到截图”。
- 实现按旧到新显示截图。
- 实现点击查看大图和左右切换。
- 为截图增加删除按钮，将文件移动到垃圾桶。
- 应用启动时清理垃圾桶中创建时间超过一个月的图片。

验收：

- 可以从详情页搜刮指定时间范围内的图片。
- 不会覆盖同名图片。
- 搜刮结果和异常情况清晰可见。
- 截图浏览顺序正确，大图查看可用。

## Milestone 5：归档和删除

目标：提供资料生命周期管理，同时保护 ROM 和外部存档。

任务：

- 实现归档操作，创建 `.archived`。
- 归档后从默认列表隐藏。
- 已归档游戏禁止搜刮截图。
- 实现删除前二次确认。
- 删除应用创建的游戏资料目录及其内部截图、存档。
- 外部存档目录不参与删除。
- 不删除 ROM 文件。
- 防止删除资料库之外的目录。

验收：

- 归档后游戏不再出现在主页。
- 归档不丢失资料、截图和存档。
- 删除提示准确，删除范围符合要求。
- 外部存档目录和 ROM 文件保持不变。

## Milestone 6：安全性和完整性检查

目标：确保文件操作不会轻易破坏用户资料。

任务：

- 对所有用户输入路径进行规范化和边界检查。
- 保存 `game.md` 时使用临时文件和原子替换。
- 文件移动失败时不报告为成功。
- 支持部分成功、部分失败的搜刮结果报告。
- 处理目录无权限、文件消失和文件名冲突。
- 检查重命名和删除操作不会越出资料库范围。
- 补充核心流程测试。
- 增加客人模式和馆长模式。
- 客人模式隐藏所有编辑和文件操作。
- 根据设备类型设置默认模式，并支持全局切换。

验收：

- 异常文件系统状态下应用不会静默丢失数据。
- 核心目录、Markdown 和截图操作有自动化测试覆盖。

## Milestone 7：第一版发布检查

目标：确认第一版范围完整、可使用。

检查项：

- 配置、启动和错误提示。
- 创建、发现、编辑和删除游戏。
- Markdown 外部编辑兼容性。
- 存档目录和节点管理。
- 截图搜刮和展示。
- 归档行为。
- ROM 和外部存档保护。
- 中文界面和空状态提示。
- 本地运行说明。

## 后续版本：存档修改

第一版完成后再单独规划，不纳入当前实现。

可能的方向：

- 通用存档编辑器外壳。
- 每个游戏独立的存档解析规则。
- Schema 驱动的字段定义。
- 修改前自动备份。
- 校验和、加密和格式验证。
- 从少数明确支持的游戏开始逐步扩展。

在没有针对具体游戏和存档格式的验证前，不承诺支持通用的自动存档修改。
