# 复古游戏博物馆设计文档

## 1. 设计目标

这是一个本地单用户 Web App，用于管理个人复古游戏资料。应用负责资料整理和展示，不负责控制 RetroArch，也不集成 LLM。

核心数据源是资料库目录和其中的 `game.md` 文件。应用启动或页面刷新时扫描资料库，读取文件内容生成页面；用户在 Web App 外修改 `game.md` 后，手动刷新即可看到最新内容。

## 2. 运行方式

- 配置文件：`~/.config/game_museum/config.yaml`
- 默认监听地址：`127.0.0.1`
- 默认端口：`7007`
- 启动时不自动打开浏览器
- 界面语言：中文

模式通过浏览器 Cookie 保存：桌面浏览器默认进入馆长模式，移动设备默认进入老总模式。老总模式只渲染资料展示，不渲染新建、编辑、搜刮、归档、删除和存档节点修改操作。

配置结构：

```yaml
screenshots_dir: /absolute/path
library_dir: /absolute/path
```

启动阶段按以下顺序校验配置：

1. 配置文件存在。
2. YAML 可以正常解析。
3. `screenshots_dir` 和 `library_dir` 均存在、是绝对路径，并且是目录。
4. 校验失败时写入终端错误并退出，不启动 Web 服务。

## 3. 目录结构

资料库中的游戏目录结构为：

```text
library_dir/
└── <platform>/
    └── <sanitized_game_name>/
        ├── game.md
        ├── screenshots/
        ├── saves/
        └── .archived                  # 仅归档游戏存在

library_dir/.trash/                       # 已删除截图的垃圾桶
```

应用只把同时满足以下条件的目录识别为游戏：

- 位于 `<library_dir>/<platform>/<game>/` 两级目录结构下。
- 存在 `game.md`。
- 不存在 `.archived`。

没有 `game.md` 的目录和存在 `.archived` 的目录均忽略。

目录名清理规则：只保留中文、英文字母、数字、连字符和下划线；其他字符替换为下划线。创建和重命名时，使用清理后的平台名和游戏名建立目录路径。

## 4. 游戏数据模型

游戏的身份由以下组合确定：

```text
platform + sanitized_game_name
```

应用内模型包含：

- `name`：游戏显示名称
- `platform`：预设平台
- `library_path`：应用创建的游戏资料目录
- `save_dir`：可选的存档目录路径
- `archived`：是否存在 `.archived`
- `description`：简介
- `note`：随记
- `save_nodes`：存档节点列表

ROM 路径不属于模型，应用不读取、不移动、不删除 ROM 文件。

## 5. `game.md` 设计

### 5.1 管理的 section

应用管理以下四个严格匹配的二级标题：

```md
## 基本资料
## 简介
## 随记
## 存档节点
```

基本生成格式：

```md
## 基本资料

- 平台：NES
- 存档目录：/absolute/path/to/saves

## 简介

这里是简介。

## 随记

这里是随记。

## 存档节点

### 节点名称

- 存档文件：save-state.ext

节点说明。
```

空值在页面中显示“没有”；文件中可以保留为空 section。

### 5.2 读写策略

不能用重新生成完整文件的方式保存，因为必须保留未知文字和未知 section。读写器需要：

- 识别四个受管理 section 的起止范围。
- 读取受管理 section 的内容。
- 保存时只替换受管理 section 的内容。
- 原样保留其他 section 和 section 外的未知文字。
- 如果受管理 section 不存在，按约定位置补充，而不是删除其他内容。

保存操作使用临时文件写入后原子替换，避免写入过程中破坏 `game.md`。

页面打开和手动刷新时重新读取文件。保存时以文件当前内容为基础，避免覆盖用户在外部做出的修改。

## 6. 存档目录和节点

存档目录路径是游戏的可选属性：

- 填写时必须是已存在的目录。
- 使用文件系统路径自动补全输入。
- 一个游戏只能绑定一个目录。
- 目录可以位于资料库外部。

存档文件只扫描绑定目录第一层的普通文件，不递归子目录。第一版不限制扩展名。

存档节点保存在 `## 存档节点` 中，按文件中的顺序展示。每个节点包含：

- 名称：必填，单行纯文本。
- 说明：可选，支持 Markdown。
- 存档文件：可选，从绑定目录的现有文件中选择。

删除节点只修改 `game.md`，不删除实际文件。文件被外部删除后，节点仍保留，并显示“文件不存在”。

## 7. 游戏发现和列表

主页扫描 `library_dir`，读取符合目录结构且没有 `.archived` 的游戏。

列表行为：

- 显示游戏名和平台。
- 按游戏名忽略大小写排序。
- 游戏名相同时按平台排序。
- 搜索只匹配游戏名，支持片段匹配并忽略大小写。
- 空库显示“没有游戏”。
- 搜索无结果显示“没有找到游戏”。

不使用额外数据库保存游戏索引；目录和 `game.md` 是事实来源。

## 8. 页面结构

### 8.1 游戏列表页

包含：

- 游戏名搜索框。
- “新建游戏”按钮。
- 游戏列表。

点击游戏进入详情页。

所有页面左上角显示当前模式和切换链接。切换后写入 Cookie，并跳转回当前页面。

### 8.2 新建游戏

表单包含：

- 游戏名输入框。
- 平台下拉菜单。
- 存档目录路径输入框。
- 简介多行文本框。
- 保存和取消按钮。

提交前校验必填字段和存档目录路径。创建成功后进入详情页。

### 8.3 游戏详情页

包含：

- 返回游戏列表按钮。
- 游戏基本资料。
- 简介编辑区和保存按钮。
- 随记编辑区和保存按钮。
- 存档节点管理区。
- 存档文件列表。
- 截图展示区。
- 时间范围下拉菜单和“搜刮截图”按钮。
- 归档和删除操作。

## 9. 截图搜刮

截图搜刮只扫描配置中的 `screenshots_dir` 第一层，并接受：

- PNG
- JPG/JPEG

时间范围选项为最近 5 分钟、10 分钟、30 分钟、60 分钟和 5 小时。

点击“搜刮截图”时记录当前时间 `now`，选择的范围为 `duration`，处理条件为：

```text
now - duration <= file_mtime <= now
```

符合条件的图片移动到当前游戏的 `screenshots/` 目录。目标目录不存在时由应用创建。

如果目标文件名已存在，保留原文件，并在文件名主体后追加递增序号，例如：

```text
shot.png
shot_1.png
shot_2.png
```

操作完成后返回结果：

- 已移动文件列表。
- 被忽略文件列表及原因。
- 没有符合条件的图片时显示“没有找到截图”。

已归档游戏不允许执行搜刮。

删除截图时将文件移动到 `library_dir/.trash/`，不直接删除。垃圾桶中的图片以文件创建时间为准，应用启动时删除创建时间超过一个月的图片。

每个游戏目录可有 `invisible.txt`，每行一个需要对老总模式隐藏的截图文件名。馆长模式读取并显示全部图片，老总模式过滤这些文件；眼睛按钮只修改该文件，不移动或删除图片。

## 10. 截图展示

- 从当前游戏的 `screenshots/` 目录读取图片。
- 按文件修改时间从旧到新排序。
- 使用缩略图展示，不显示文件名。
- 点击缩略图打开大图查看器。
- 大图查看器支持上一张和下一张。
- 没有截图时显示“没有”。
- 每张截图提供删除按钮，删除操作将图片移入垃圾桶。

游戏目录下的 `creative/` 目录作为独立图片来源，第一层仅读取 PNG、JPG/JPEG，并在详情页“二创”区块展示；不参与截图搜刮、隐藏和删除操作。

## 11. 截图修复工具

馆长模式详情页通过“工具”入口进入当前游戏的工具页。工具页从当前游戏的 `screenshots/` 读取 PNG、JPG/JPEG；待修复列表排除文件名主体已经以 `_fixed` 结尾的文件，所有已修复文件在下方单独展示并支持选择后移入垃圾桶。

用户勾选图片后批量执行修复。后端使用 OpenCV 查找面积最大的合理四边形作为屏幕边界，按四角进行透视变换，再裁掉四周连续黑边，只保留实际显示区域；不做颜色、亮度和反光处理。输出尺寸依据识别出的边长计算，不强制统一分辨率。

输出写入原 `screenshots/` 目录，使用 `<stem>_fixed<suffix>` 命名；发生冲突时追加 `_1`、`_2` 等序号。原图不修改。每张图片独立处理，失败只记录该图片的失败原因，其他图片继续；成功文件返回给前端并加入截图列表，同时在工具页下方显示结果缩略图。已修复图片可从工具页选择并永久删除，不进入垃圾桶。

## 11. 归档和删除

### 11.1 归档

归档操作只需在游戏目录创建 `.archived` 文件。创建成功后立即从当前列表隐藏，不需要二次确认。

应用不提供已归档列表和恢复功能。删除 `.archived` 后，下一次刷新会使游戏重新显示。

### 11.2 删除

删除必须二次确认。确认文案需要明确：

- 会删除应用创建的游戏资料目录、截图和资料库内的存档。
- 不会删除 ROM 文件。
- 如果存档路径在资料库外，不会删除外部存档目录及文件。

删除范围只允许是应用创建并识别出的游戏资料目录，不能根据用户填写的外部存档路径执行删除。

## 12. 一致性和错误处理

- 所有路径操作使用规范化后的绝对路径。
- 目录不存在、路径类型不正确或无权限时显示明确错误。
- 文件移动失败时，不应报告为成功移动。
- 部分文件移动成功、部分失败时，结果中分别列出成功和失败项。
- 文件名冲突时不得覆盖目标文件。
- 外部删除或修改文件后，页面刷新应反映实际文件状态。
- 删除游戏时必须避免误删资料库之外的目录。

## 13. 非目标

第一版不包含：

- RetroArch 启动、控制或状态读取。
- ROM 管理。
- ChatGPT 或 LLM 对话。
- 多用户、账号和权限系统。
- 局域网访问配置。
- 资料目录递归扫描。
- 存档扩展名配置。
- 已归档游戏列表和恢复功能。
- 文件实时监控。
- 存档文件上传或下载。
