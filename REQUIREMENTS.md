# 复古游戏博物馆

本项目是一个本地单用户 Web App，用于管理个人复古游戏博物馆。

## 项目范围

- 不控制 RetroArch，游戏操作由用户手动完成。
- 不集成 ChatGPT 或其他 LLM 功能。
- 默认只监听本机。
- 默认端口为 `7007`。
- 启动时不自动打开浏览器。

## 配置

配置文件固定为 `~/.config/game_museum/config.yaml`：

```yaml
screenshots_dir: /absolute/path
library_dir: /absolute/path
```

要求：

- 两个路径都必须是绝对路径，且目录必须已存在。
- 配置文件不存在、格式错误或字段缺失时，应用启动失败，并只在终端输出错误。
- 修改配置后需要重启应用。
- 平台列表和截图格式列表写死在代码中。平台包括 NES、SNES、GB、GBA、N64、PS1、PS2、NDS、3DS、Arcade。

## 平台

预设平台：NES、SNES、GB、GBA、N64、PS1、PS2、Arcade。平台使用下拉菜单。

## 游戏资料

新建游戏时，游戏名和平台必填。新建表单提供“取消”按钮。

游戏资料目录结构：

```text
<library_dir>/<platform>/<gamename>/
├── game.md
├── screenshots/
└── saves/
```

创建游戏时立即创建资料目录。目录名只保留中文、英文字母、数字、连字符和下划线，其余字符替换为下划线。`平台 + 清理后的游戏名`作为唯一标识；冲突时禁止创建并提示游戏已存在。

不管理 ROM 路径，也不删除 ROM 文件。

游戏创建后，游戏名、平台和简介可以在详情页修改；修改游戏名或平台时同步调整资料目录路径和 `game.md`。

## `game.md`

文件固定包含以下 section：

```md
## 基本资料

## 简介

## 随记

## 存档节点
```

- 基本资料中不显示 ROM。
- 存档固定使用游戏资料目录下自动创建的 `saves/`。
- 简介和随记支持 Markdown。
- 随记只有一份，编辑后直接覆盖，不保留历史，也不记录日期。
- 没有内容的 section 显示“没有”。
- 存档节点按 `game.md` 中的书写顺序显示。
- 应用保存时必须保留未知文字和未知 section，不能整文件覆盖。
- 外部修改 `game.md` 后，手动刷新页面即可读取最新内容；不做实时文件监控。

## 存档

应用只扫描游戏资料目录下 `saves/` 的第一层普通文件，不递归子目录；第一版暂不限制存档文件扩展名。

详情页显示存档目录中的存档文件名，点击文件名复制完整路径，不提供下载。

每个存档节点包含节点名称、说明和可选的关联存档文件。节点名称必填、使用普通单行输入且不支持 Markdown；说明支持 Markdown。关联文件只能从绑定存档目录中选择一个文件。

支持新增、编辑和删除节点。删除节点只删除 `game.md` 中的信息，不删除实际存档文件。关联文件不存在时显示“文件不存在”；没有关联文件时显示“没有关联存档文件”。

## 游戏列表和详情页

主页显示所有未归档游戏、游戏名和平台，提供“新建游戏”按钮，并支持按游戏名片段搜索（忽略大小写）。第一版只搜索游戏名。无游戏时显示“没有游戏”，无搜索结果时显示“没有找到游戏”。列表按游戏名忽略大小写排序；游戏名相同时按平台排序。

详情页显示基本资料、简介、随记、存档节点、截图和存档文件；没有内容时显示“没有”。详情页提供返回游戏列表按钮。

简介和随记使用普通多行文本框编辑，点击“保存”后按 Markdown 渲染。存档节点说明同样按 Markdown 渲染。

## 截图

截图来源为全局 `screenshots_dir`，只扫描第一层。第一版只处理 PNG、JPG/JPEG，其他文件忽略。

每个游戏目录下如果存在 `creative/` 目录，则将其中第一层的 PNG、JPG/JPEG 图片显示在独立的“二创”区块中；二创图片不参与截图搜刮和截图隐藏过滤。

馆长模式详情页提供“工具”入口。工具页显示当前游戏 `screenshots/` 下所有 PNG、JPG/JPEG 图片；待修复列表不显示文件名以 `_fixed` 结尾的已修复图片，已修复图片在下方单独列出。馆长可以选择多张待修复图片并点击“修复”，也可以选择已修复图片并将其移到垃圾桶。

修复使用自动屏幕边缘识别：识别屏幕四角，进行透视校正并裁掉屏幕外区域和四周连续黑边；随后自动减弱横向扫描线和摩尔纹，并进行轻度去噪、保边锐化。输出按识别出的屏幕比例生成，不强制统一分辨率，不处理反光或亮度不均。结果保存回 `screenshots/`，在原文件名主体后添加 `_fixed`，覆盖同名的旧 `_fixed` 文件，原图不变。修复后的图片自动进入截图列表，并在工具页下方以缩略图显示；已修复图片可在工具页选择并永久删除，不进入垃圾桶。自动识别失败时跳过该图片并显示失败原因，其他选中图片继续处理。工具页显示每张图片的处理结果。

详情页提供“搜刮截图”按钮和时间范围下拉菜单：最近 5 分钟、10 分钟、30 分钟、60 分钟、5 小时。

搜刮时，针对当前游戏，按点击按钮时的当前时间向前回溯，并按文件修改时间判断。只处理当前时间之前的图片，将其移动到 `<game_dir>/screenshots/`。目标文件同名时追加递增序号，不覆盖原文件。

没有符合条件的图片时显示“没有找到截图”，不移动任何文件。完成后显示移动的文件、忽略的文件及原因。已归档游戏禁止搜刮截图，并提示“游戏已归档”。

截图按文件修改时间从旧到新显示，不显示文件名。点击缩略图查看大图，大图支持左右切换。

如果同一张截图存在 `_fixed` 修复版，详情页优先显示修复版，不同时显示原图；馆长模式的修复版显示扳手图标，游客模式不显示该图标。工具页仍显示原图和全部修复版。

馆长模式下可以通过拖拽截图调整显示顺序，顺序保存到游戏目录的 `screenshot_order.txt`。新截图追加到末尾，没有排序记录的图片按原有修改时间顺序排列。

截图支持删除。删除不会直接删除文件，而是将图片移动到 `library_dir/.trash/` 垃圾桶目录。应用启动时执行垃圾桶清理，删除其中创建时间早于一个月的图片。

馆长模式下每张截图提供眼睛图标，用于切换是否在游客模式显示。隐藏的截图不会删除；文件名记录在游戏目录的 `invisible.txt` 中。

## 归档和删除

归档时在游戏目录中创建 `.archived`。资料、截图和存档全部保留，但游戏从默认列表完全隐藏，不提供“已归档”入口或恢复功能，也不需要二次确认。`.archived` 不存在时视为未归档。

删除前需要二次确认。删除应用创建的游戏资料目录、其中的截图和存档，但不删除 ROM 文件。如果存档路径位于资料库外部，则保留外部存档目录及文件。只删除应用实际创建的游戏资料目录。

## 界面语言

Web App 界面使用中文。

## 使用模式

Web App 有两种全局模式：

- 馆长模式：保留创建、编辑、搜刮截图、归档和删除等操作。
- 游客模式：只读，隐藏所有编辑和文件操作。

每页左上角提供模式切换。模式对所有页面生效，并通过浏览器 Cookie 保持。

默认模式：桌面浏览器为馆长模式，手机浏览器为游客模式。
